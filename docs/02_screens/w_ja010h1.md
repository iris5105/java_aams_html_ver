# w_ja010h1 : 자산명세표(계좌별) 데이터 조회 및 리포트 연동 가이드

## 1. 화면 개요 및 파워빌더 대응 정보

- **프로그램 ID**: `W_JA010H1` (w_ja010h1)
- **메뉴 경로**: 사무관리 > 자문일일 > 자산명세표 (계좌별)
- **파워빌더 소스**: `pb_recource/JA010/w_ja010h1.srw`
- **데이터윈도우 / 리포트**:
  - 마스터 목록: `d_szm0ia.srd` (계좌 목록)
  - 리포트 템플릿: `rd_ja010h1.mrd`
- **화면 구조**: 좌측 펀드 목록 그리드 + 우측 리포트 뷰어(`<iframe>`), 반응형 모바일 모달 지원

---

## 2. 데이터 조회 및 렌더링 전체 시퀀스

```
[화면 진입 / 기준일자 변경]
            │
            ▼ (1)
[w_ja010h1.html :: loadFunds()]
  - API 호출: GET /api/daily/ja010h1/funds?corpGr=...&ymd=...
            │
            ▼ (2)
[Ja010h1Controller.java :: getFundList()]
  - Ja010h1Service.getFundList(corpGr, ymd)
  - Ja010h1Mapper.selectFundList(corpGr, ymd)
            │
            ▼ (3)
[그리드 데이터 바인딩 및 첫 번째 행 선택]
  - rows[0].select() -> syncDetail(rows[0], data[0])
            │
            ▼ (4)
[w_ja010h1.html :: syncDetail()]
  - rawUrl = "/api/daily/ja010h1/preview?corpGr=...&ymd=...&fundCd=...&fundNm=..."
  - previewUrl = AamsReport.formatPreviewUrl(rawUrl) -> 120% 줌 부착
  - iframe.src = previewUrl
            │
            ▼ (5)
[Ja010h1Controller.java :: previewReport()]
  - RdReportService.generateJa010h1Report(corpGr, ymd, fundCd, fundNm, "pdf")
  - Crownix 엔진 구동 -> PDF 바이너리 스트림 반환
```

---

## 3. 계층별 세부 코드 및 구성 분석

### 3.1 화면 및 클라이언트 스크립트 ([w_ja010h1.html](file:///d:/work/java_aams_html_ver/src/main/resources/templates/views/daily/w_ja010h1.html))

- **삼중 안전망 및 120% 줌 연동**:
  ```javascript
  setupTabulatorRowSelection(grid, function(row, data) {
      syncDetail(row, data);
  });
  grid.on("rowClick", function(e, row) {
      syncDetail(row, row ? row.getData() : null);
  });
  grid.on("rowSelectionChanged", function(data, rows) {
      if (rows && rows.length > 0) syncDetail(rows[0], rows[0].getData());
  });

  function syncDetail(row, data) {
      var rawPreviewUrl = "/api/daily/ja010h1/preview?corpGr=" + encodeURIComponent(corpGr) +
          "&ymd=" + encodeURIComponent(ymd) +
          "&fundCd=" + encodeURIComponent(fundCd) +
          "&fundNm=" + encodeURIComponent(fundNm) +
          "&t=" + new Date().getTime();
      
      iframe.src = AamsReport.formatPreviewUrl(rawPreviewUrl);
  }
  ```

### 3.2 컨트롤러 계층 (`Ja010h1Controller.java`)

```java
@GetMapping("/api/daily/ja010h1/preview")
public ResponseEntity<byte[]> previewReport(
        @RequestParam(name = "corpGr", required = false) String corpGr,
        @RequestParam("ymd") String ymd,
        @RequestParam("fundCd") String fundCd,
        @RequestParam(name = "fundNm", required = false) String fundNm) {
    RdReportService.ExportResult exportResult = rdReportService.generateJa010h1Report(
            corpGr, ymd, fundCd, fundNm, "pdf");

    return ResponseEntity.ok()
            .contentType(MediaType.APPLICATION_PDF)
            .header(HttpHeaders.CONTENT_DISPOSITION, "inline; filename=\"ja010h1.pdf\"")
            .body(exportResult.getData());
}
```

---

## 4. 유지보수 체크포인트

1. **새 창 열기 버튼 동작**:
   - 상단 헤더의 `btnOpenNewWindow` 클릭 시 `AamsReport.formatPreviewUrl(rawUrl)`을 통해 새 탭에서도 120% 확대 비율이 유지되는지 확인합니다.
2. **새로고침 초기화**:
   - `currentPane.onRefresh()` 호출 시 마스터 그리드 데이터 초기화와 iframe의 `src = "about:blank"` 처리가 정상 작동하는지 점검합니다.
